#import "/template.typ": (
  Assign, IfElseChain, Return, While, algorithm, capfig, capsubfig, captab, multicite, nwpu-thesis, zh
)

#show: nwpu-thesis.with(
  anonymous: false, // 是否开启盲审模式
  info: (
    title: ("基于Typst的西工大论文模板", "长标题支持"),
    author: "航小天",
    major: "计算机科学与技术",
    supervisor: ("张三", "教授"),
    submit-date: (year: 2026, month: 3),
  ),
  abstract: (
    content: [
      听觉虚拟又可称为可听化，是近年来随着声学仿真技术的发展而出现的新概念，即通过对包含单个（或多个）声源的声场进行物理或数学建模，以达到模拟空间听音效果的目的。若考虑双耳效应，则可称为双耳听觉虚拟（Binaural Modeling）。

      ……
    ],
    keywords: ("听觉虚拟", "HRTF", "神经网络"),
  ),
  abstract-en: (
    content: [
      Virtual auditory technology is also called auralization. It is brought forward as a new concept with the development of acoustic simulation techniques in recent years and can be implemented by establishing the physical or mathematical models of a sound field to achieve sound effects simulation. If we consider the binaural effect, it can be called binaural virtual auditory.

      ……..
    ],
    keywords: ("virtual auditory", "HRTF", "neural network"),
  ),
  appendix: [
    #align(center)[*附录 A 完整符号表*]

    #heading(level: 2, numbering: none)[A.1 集合、索引与规模参数]

    #table(
      columns: (auto, 1fr, auto),
      table.header([符号], [含义], [单位]),
      [$cal(U) = brace.l 1, 2, dots, N brace.r$], [UAV 节点集合], [-],
      [$cal(K) = brace.l 1, 2, dots, K brace.r$], [地面任务节点集合], [-],
      [$s$], [Sink 节点], [-],
      [$cal(V) = cal(U) union brace.l s brace.r$], [通信节点集合], [-],
      [$cal(T) = brace.l 0, 1, dots, T - 1 brace.r$], [离散时隙集合], [-],
      [$cal(G) = brace.l 0, 1, dots, G - 1 brace.r$], [控制段集合], [-],
      [$N$], [UAV 数量], [-],
      [$K$], [地面任务节点数量], [-],
      [$T$], [总时隙数], [-],
      [$L_c$], [单个控制段包含的时隙数], [-],
      [$G = ceil(T / L_c)$], [控制段总数], [-],
      [$D = 3 N G$], [单个候选解维度], [-],
      [$i, j$], [UAV 或通信节点索引], [-],
      [$k$], [地面任务节点索引], [-],
      [$t$], [时隙索引], [-],
      [$g$], [控制段索引], [-],
      [$cal(T)_g$], [第 $g$ 个控制段对应的时隙集合], [-],
    )

    其中，

    $
    cal(T)_g =
    brace.l
      g L_c,
      g L_c + 1,
      dots,
      min((g + 1) L_c - 1, T - 1)
    brace.r.
    $

    #heading(level: 2, numbering: none)[A.2 场景输入参数]

    #table(
      columns: (auto, 1fr, auto),
      table.header([符号], [含义], [单位]),
      [$L$], [正方形任务区域边长], [m],
      [$Delta t$], [单个时隙长度], [s],
      [$bold(q)_k = [x_k^"g", y_k^"g"]^top$], [地面任务节点 $k$ 的位置], [m],
      [$bold(p)_s = [x_s, y_s]^top$], [Sink 节点位置], [m],
      [$bold(p)_i(0)$], [UAV $i$ 的初始位置], [m],
    )

    #heading(level: 2, numbering: none)[A.3 优化变量]

    #table(
      columns: (auto, 1fr, auto),
      table.header([符号], [含义], [单位]),
      [$bold(u)_i(g)$], [UAV $i$ 在控制段 $g$ 的控制向量], [-],
      [$hat(v)_i(g)$], [UAV $i$ 在控制段 $g$ 的期望飞行速度], [m/s],
      [$hat(theta)_i(g)$], [UAV $i$ 在控制段 $g$ 的期望航向角], [rad],
      [$P_i^"tx"(g)$], [UAV $i$ 在控制段 $g$ 的发射功率], [W],
      [$bold(U)$], [完整优化变量集合], [-],
    )

    其中，

    $
    bold(u)_i(g)
    =
    [
      hat(v)_i(g),
      hat(theta)_i(g),
      P_i^"tx"(g)
    ],
    $

    $
    bold(U)
    =
    brace.l
      bold(u)_i(g)
      mid
      i in cal(U),
      g in cal(G)
    brace.r.
    $

    #heading(level: 2, numbering: none)[A.4 运动与安全参数]

    #table(
      columns: (auto, 1fr, auto),
      table.header([符号], [含义], [单位]),
      [$v_i^"max"$], [UAV $i$ 的最大飞行速度], [m/s],
      [$a^"max"$], [最大切向加速度], [m/s^2],
      [$Delta theta^"max"$], [单时隙最大航向变化角], [rad],
      [$omega^"max"$], [最大角速度], [rad/s],
      [$R^"min"$], [最小转弯半径], [m],
      [$d_"safe"$], [UAV 间最小安全距离], [m],
    )

    #heading(level: 2, numbering: none)[A.5 通信、覆盖与队列参数]

    #table(
      columns: (auto, 1fr, auto),
      table.header([符号], [含义], [单位]),
      [$B_i$], [UAV $i$ 的通信带宽], [Hz],
      [$P_i^"max"$], [UAV $i$ 的最大发射功率], [W],
      [$beta_0$], [参考距离增益系数], [m^2],
      [$delta$], [信道增益修正项], [m^2],
      [$sigma^2$], [噪声功率], [W],
      [$R_i^"cov"$], [UAV $i$ 的覆盖半径], [m],
      [$lambda_k$], [地面任务节点 $k$ 的数据产生率], [bit/s],
      [$Q_i^"max"$], [UAV $i$ 的最大缓存容量], [bit],
      [$epsilon_"con"$], [最小数据泄放比例系数], [-],
    )

    #heading(level: 2, numbering: none)[A.6 能量参数]

    #table(
      columns: (auto, 1fr, auto),
      table.header([符号], [含义], [单位]),
      [$E_i^"max"$], [UAV $i$ 的最大能量容量], [J],
      [$P_i^"hover"$], [UAV $i$ 的悬停功率], [W],
      [$c_(2, i)^"fly"$], [UAV $i$ 的速度平方项飞行功率系数], [W/(m/s)^2],
      [$c_(3, i)^"fly"$], [UAV $i$ 的速度立方项飞行功率系数], [W/(m/s)^3],
    )

    各 UAV 初始能量满足

    $
    E_i(0) = E_i^"max".
    $

    #heading(level: 2, numbering: none)[A.7 状态变量]

    #table(
      columns: (auto, 1fr, auto),
      table.header([符号], [含义], [单位]),
      [$bold(p)_i(t) = [x_i(t), y_i(t)]^top$], [UAV $i$ 在时隙 $t$ 的二维位置], [m],
      [$v_i(t)$], [UAV $i$ 在时隙 $t$ 的实际执行速度], [m/s],
      [$phi_i(t)$], [UAV $i$ 在时隙 $t$ 的实际执行航向角], [rad],
      [$Q_i(t)$], [UAV $i$ 在时隙 $t$ 的缓存队列长度], [bit],
      [$E_i(t)$], [UAV $i$ 在时隙 $t$ 的剩余能量], [J],
    )

    #heading(level: 2, numbering: none)[A.8 通信、覆盖与队列中间量]

    #table(
      columns: (auto, 1fr, auto),
      table.header([符号], [含义], [单位]),
      [$d_(i j)(t)$], [UAV $i$ 与 UAV $j$ 在时隙 $t$ 的距离], [m],
      [$d_(i s)(t)$], [UAV $i$ 与 Sink 在时隙 $t$ 的距离], [m],
      [$d_(i k)^"g"(t)$], [UAV $i$ 与地面任务节点 $k$ 在时隙 $t$ 的距离], [m],
      [$g_(i j)(t)$], [链路 $i -> j$ 的信道增益], [-],
      [$gamma_(i j)(t)$], [链路 $i -> j$ 的信噪比], [-],
      [$r_(i j)(t)$], [链路 $i -> j$ 的数据传输速率], [bit/s],
      [$c_(i k)(t)$], [UAV $i$ 对地面任务节点 $k$ 的覆盖指示量], [-],
      [$z_k(t)$], [地面任务节点 $k$ 在时隙 $t$ 的覆盖状态], [-],
      [$A_i(t)$], [时隙 $t$ 进入 UAV $i$ 队列的数据量], [bit],
      [$F_i^"in"(t)$], [UAV $i$ 在时隙 $t$ 的等效转发流入量], [bit],
      [$F_i^"out"(t)$], [UAV $i$ 在时隙 $t$ 的等效转发流出量], [bit],
    )

    其中，

    $
    c_(i k)(t) in brace.l 0, 1 brace.r,
    quad
    z_k(t) in brace.l 0, 1 brace.r.
    $

    #heading(level: 2, numbering: none)[A.9 评价指标与目标函数符号]

    #table(
      columns: (auto, 1fr, auto),
      table.header([符号], [含义], [单位]),
      [$rho_"cov"$], [任务周期平均覆盖率], [-],
      [$R_"norm"$], [归一化系统吞吐量], [-],
      [$Q_"norm"$], [归一化队列负载], [-],
      [$E_"norm"$], [归一化能耗指标], [-],
      [$V_"cov"^"norm"$], [归一化覆盖违约指标], [-],
      [$V_"con"^"norm"$], [归一化连通性违约指标], [-],
      [$J_"perf"$], [通信性能评价项], [-],
      [$J_"total"$], [综合目标函数值], [-],
    )

    #heading(level: 2, numbering: none)[A.10 目标函数权重]

    #table(
      columns: (auto, 1fr, auto),
      table.header([符号], [含义], [单位]),
      [$lambda_0$], [吞吐量指标权重], [-],
      [$lambda_1$], [队列指标权重], [-],
      [$lambda_2$], [能耗指标权重], [-],
      [$lambda_3$], [覆盖违约指标权重], [-],
      [$lambda_4$], [连通性违约指标权重], [-],
    )

    #pagebreak()
    #align(center)[*附录 B 实验补充参数设置*]

    #heading(level: 2, numbering: none)[B.1 通信模型参数]

    本文通信模型采用基于二维距离平方衰减的简化视距信道模型，其主要参数设置如表 B-1 所示。

    #table(
      columns: (1fr, auto),
      table.header([参数], [数值]),
      [信道增益参数 $beta_0$], [$1 times 10^(-3)$],
      [噪声功率], [$1 times 10^(-9)$],
      [连通违约系数 $epsilon$], [0.1],
      [任务节点单时隙数据量], [$5 times 10^4$ bit],
    )

    #heading(level: 2, numbering: none)[B.2 UAV 动力学约束参数]

    无人机运动过程中采用固定高度二维运动模型，并考虑速度、加速度、转角及安全距离等动力学约束，其相关参数如表 B-2 所示。

    #table(
      columns: (1fr, auto),
      table.header([参数], [数值]),
      [安全距离 $D_"safe"$], [20 m],
      [最大加速度], [5 m/s^2],
      [最大转角], [30°],
      [最大角速度], [30°/s],
      [最小转弯半径], [30 m],
    )

    #heading(level: 2, numbering: none)[B.3 CSA-L-SHADE 状态控制参数]

    CSA-L-SHADE 采用覆盖状态感知参数控制机制，根据种群覆盖状态动态调整差分进化参数 $p / F / "CR"$。其状态机相关参数设置如表 B-3 所示。

    #table(
      columns: (1fr, auto),
      table.header([参数], [数值]),
      [`elite_ratio`], [0.20],
      [$tau_H$], [0.15],
      [$tau_L$], [0.10],
      [$tau_"safe"$], [0.10],
      [$tau_"recover"$], [0.11],
      [$r_L$], [0.20],
      [$r_H$], [0.50],
      [$r_"recover"$], [0.40],
      [$delta_"stable"$], [0.002],
      [`hold_required`], [3],
    )

    CSA-L-SHADE 在不同覆盖状态下采用不同的 $p / F / "CR"$ 取值范围，其参数配置如表 B-4 所示。

    #table(
      columns: (auto, auto, auto, auto),
      table.header([状态], [$p$], [$F$ 范围], [$"CR"$ 范围]),
      [$S_1$], [0.08], [$[0.50, 0.80]$], [$[0.15, 0.40]$],
      [$S_2$], [0.10], [$[0.35, 0.60]$], [$[0.10, 0.30]$],
      [$S_3$], [0.15], [$[0.20, 0.50]$], [$[0.10, 0.35]$],
      [$S_4$], [0.08], [$[0.35, 0.60]$], [$[0.10, 0.25]$],
    )

    其中，$S_1$、$S_2$、$S_3$ 与 $S_4$ 分别表示覆盖探索状态、覆盖改进状态、覆盖稳定状态与覆盖恢复状态。
  ],
  acknowledgement: [
    致谢内容……
  ],
  design-summary: [
    小结内容……
  ],
)

= 绪论

== 研究背景与意义
无人机（Unmanned Aerial Vehicle, UAV）因具备快速部署、机动覆盖和空中中继能力，已成为灾后应急通信的重要补充手段@HBGL202514005。在地震、洪涝、山火等场景中，地面通信基础设施可能受损，UAV 可作为临时空中通信节点，为地面任务区域提供覆盖并将数据回传至 Sink 节点。然而，应急通信任务并非简单的静态覆盖问题。灾后初期，地面任务节点的数据流通常具有突发性与持续性，若 UAV 网络不能在早期形成有效覆盖与回传结构，数据将持续积压于本地节点或 UAV 缓存队列中，进而引发时延升高、缓存压力增加和有效数据丢失等问题。因此，UAV 通信网络路由规划需要同时考虑覆盖结构的快速建立、队列状态的动态演化以及面向 Sink 的持续回传能力。

随着应用场景复杂度提高，UAV 通信网络已不再是单一航迹规划或静态部署问题，而逐渐演变为轨迹控制、任务覆盖、链路传输、队列演化和能量消耗相互耦合的动态优化问题@2024Evolutionary。UAV 的速度、航向角和位置变化决定其与地面任务节点、其他 UAV 及 Sink 节点之间的空间距离，进而影响覆盖关系、信道增益和链路传输速率；覆盖状态决定地面数据能否进入 UAV 队列，而队列能否及时泄放又依赖于 UAV–UAV 链路和 UAV–Sink 链路的回传能力；同时，高速飞行和高功率发射会加速能量消耗，剩余能量又反过来限制 UAV 后续运动和通信能力。由此可见，轨迹、覆盖、队列、回传和能耗之间存在明显闭环耦合，任一环节的局部优化都可能影响系统整体性能@2024Evolutionary。

在多 UAV 协同场景下，上述问题还会因 UAV 异构性进一步复杂化。不同 UAV 在最大速度、覆盖半径、通信带宽、发射功率、缓存容量和初始能量等方面存在差异，其适合承担的覆盖、巡航、中继或回传任务也不同。因此，异构 UAV 集群路由规划需要在有限资源条件下协调不同 UAV 的运动控制、覆盖服务、数据回传和能量消耗，而不能简单采用统一控制策略或割裂式优化方法@9417264。

从优化求解角度看，该问题具有高维、非线性、非光滑和强时序耦合等特点。当前时隙的控制决策会通过位置、队列和剩余能量继续影响后续系统状态；覆盖关系由距离阈值触发，具有离散跳变特征；通信速率又由距离衰减信道和发射功率共同决定。因此，传统精确优化方法在该类问题上往往面临建模复杂、计算量大和可扩展性不足等困难@2025UAV，需要采用启发式或元启发式算法进行近似求解@Das2026@AGRAWAL20241。

差分进化及其改进算法在连续变量、高维和复杂约束优化问题中具有较好的适用性。L-SHADE@LI2022350 在 SHADE@6900380 成功历史参数记忆机制基础上引入线性种群规模缩减策略，能够在搜索前期保持探索能力，并在后期增强收敛效率，因而适合作为 UAV 控制序列优化的基础框架。然而，原始 L-SHADE 本质上仍是通用连续优化算法，其参数自适应主要依赖历史成功经验，并不显式感知 UAV 通信网络中的覆盖状态。对于动态覆盖路由问题而言，覆盖状态不仅是目标函数中的惩罚项，还决定地面数据到达、队列演化和后续通信性能。因此，有必要在 L-SHADE 框架中引入 UAV 覆盖结构信息，设计面向覆盖状态的改进机制。

基于上述背景，本文围绕异构无人机集群通信网络动态路由规划问题展开研究，在考虑 UAV 运动约束、通信能力、地面节点覆盖、队列演化和能量消耗的基础上，建立统一的动态优化模型，并提出覆盖状态感知 L-SHADE 算法，即 CSA-L-SHADE（Coverage-State-Aware L-SHADE）。该算法以 L-SHADE 为基础框架，通过覆盖优先初始化改善初始搜索入口，并通过覆盖状态感知参数控制机制动态调节搜索行为，从而提升算法在 UAV 动态覆盖路由问题中的覆盖保持能力和综合优化性能。

本文研究具有一定理论意义和工程意义。理论上，本文将异构 UAV 的运动控制、通信链路、覆盖状态、数据队列和能耗约束纳入统一建模框架，有助于刻画 UAV 动态路由规划中覆盖、回传、吞吐、队列和能量之间的耦合关系；工程上，本文所提出的覆盖状态感知优化方法能够在有限计算预算下生成可执行的 UAV 控制序列，为灾后应急通信、临时网络部署和无人机协同中继提供可复现的仿真分析方法和决策参考。

== 国内外研究现状
=== UAV 通信网络与动态路由规划研究
UAV 通信网络通常将无人机作为空中通信节点，通过 UAV–地面节点链路、UAV–UAV 链路以及 UAV–Sink 链路构建临时通信网络@HNLG202512002。现有研究早期多关注单架 UAV 的部署位置、飞行轨迹或空地信道建模@1025020518.nh，主要目标包括扩大地面覆盖范围、提高通信速率或降低飞行能耗。随着多 UAV 协同任务和应急通信需求增长，研究重点逐渐从单机轨迹优化扩展到多 UAV 协同路径规划、覆盖控制、数据回传和资源分配等方向@HLYZ201809007。

在 UAV 通信网络中，覆盖优化与回传能力是两个相互耦合的核心问题。覆盖优化侧重于通过 UAV 部署或轨迹规划，使更多地面任务节点获得有效服务；回传能力则关注 UAV 采集到的数据能否通过 UAV–UAV 链路或 UAV–Sink 链路及时汇聚至 Sink 节点。若 UAV 为追求覆盖率而过度分散，可能导致回传链路能力不足；若 UAV 为增强回传能力而过度靠近 Sink 或彼此聚集，又可能降低边缘任务节点覆盖水平@10535707。因此，覆盖与回传之间的协同优化是 UAV 应急通信规划中的关键问题@2024Evolutionary。

已有研究通常采用图论连通@1024503963.nh、通信半径、k-连通、流约束或链路可靠性等方法刻画 UAV 网络结构@1022589382.nh。这类方法适合描述网络拓扑可达性，但在动态通信任务中，拓扑可达并不必然意味着数据能够稳定回传。链路速率、队列状态和 Sink 汇聚能力同样会影响网络实际服务质量。因此，动态 UAV 路由规划不仅需要考虑拓扑连通，还需要进一步关注数据在 UAV 队列中的积压和泄放过程。

总体来看，现有 UAV 路径与路由研究已经从单纯的几何路径优化扩展到轨迹、链路、功率、队列和能耗共同作用的综合优化问题。然而，部分协议层路由研究更关注下一跳选择，对 UAV 连续运动控制、覆盖状态和队列演化之间的耦合刻画不足@10041916；部分轨迹优化研究虽然考虑通信速率或能耗，但对覆盖状态如何影响数据到达和系统稳定性的关注仍不充分@ELNABTY2022101564。因此，有必要从动态覆盖路由角度重新组织问题模型，将 UAV 控制、覆盖状态、通信速率和队列演化统一考虑。

=== 元启发式算法与 L-SHADE 研究
针对 UAV 路径规划、覆盖优化和通信资源分配等问题，元启发式算法因其实现灵活、对目标函数形式要求较低、易于处理复杂约束而被广泛使用。GA 适合离散路径、访问顺序和任务分配问题，具有较强编码灵活性，但收敛速度和参数设置对结果影响较大@湛文静2023基于改进遗传算法的路径规划问题相关研究综述；PSO 适合连续航点、悬停点和速度参数优化，具有实现简单、早期收敛快等优点，但在复杂约束和高维问题中容易早熟@刘秀梅2016动态系统中粒子群优化算法综述；DE 及其改进算法则适合连续实数编码、多约束和黑箱优化问题，在 UAV 轨迹、功率和资源联合优化中具有较强适用性@杨启文2008差分进化算法综述。

L-SHADE 是差分进化算法的重要改进版本，其核心思想包括成功历史参数自适应和线性种群规模缩减@LI2022350。成功历史参数记忆机制能够根据以往成功试验个体的缩放因子 F 和交叉概率 CR 调整后续参数采样，从而减少人工调参依赖；线性种群规模缩减机制则使算法在前期保持较大的种群规模以增强探索能力，在后期逐步减小种群规模以提高收敛效率。这些特点使 L-SHADE 在连续优化和高维黑箱问题中表现出较好的鲁棒性。

=== 现有研究不足与本文切入点
综合已有研究可以看出，UAV 通信网络动态路由规划仍存在以下不足。

第一，现有研究已广泛关注 UAV 轨迹、功率、吞吐量和能耗之间的联合优化，也有部分工作将动态任务到达和队列稳定性纳入 UAV 轨迹或资源优化框架中@7888557。然而，这些研究多从通信资源分配、MEC 卸载或吞吐最大化角度展开，对“覆盖状态—数据到达—队列演化—Sink 回传能力”之间的闭环关系刻画仍不充分。

第二，FANET 路由研究通常围绕拓扑路由、位置路由、分簇路由、DTN 路由和策略型路由展开，主要关注动态拓扑下的数据包转发、下一跳选择和链路维护@10041916。这类方法适合协议层路由优化，但难以直接刻画 UAV 连续运动控制、链路速率变化和队列泄放过程。

第三，L-SHADE 的参数自适应主要来自 SHADE 的成功历史参数记忆@6900380，并通过线性种群规模缩减改善搜索效率@LI2022350。已有研究也表明 L-SHADE 及其变体可用于 UAV 资源配置等高维连续优化问题，但其原始机制并不针对 UAV 覆盖状态设计，难以根据 coverage state 主动调节搜索行为。

基于上述不足，本文以异构 UAV 动态覆盖路由规划为研究对象，建立包含运动、通信、覆盖、队列和能耗的统一模型，并在 L-SHADE 框架基础上提出 CSA-L-SHADE 算法。该算法通过覆盖优先初始化改善初始搜索入口，通过覆盖状态感知参数控制机制调节搜索行为，从而增强算法对 UAV 覆盖路由问题结构的适配能力。
== 本文主要研究内容
围绕异构无人机集群通信网络动态路由规划问题，本文主要开展以下研究工作。

建立异构 UAV 动态覆盖路由规划模型。本文考虑由多架异构 UAV、若干地面任务节点和固定 Sink 节点构成的通信网络，建立 UAV 运动模型、无线通信模型、地面节点覆盖模型、数据队列与功能性连通表征模型以及能量消耗模型。在此基础上，将系统吞吐量、队列负载、能量消耗、覆盖违约和连通违约统一纳入综合目标函数，形成面向 UAV 控制序列的动态优化问题。

提出覆盖优先初始化机制。针对原始 L-SHADE 随机初始化难以快速形成有效覆盖结构的问题，本文利用地面任务节点空间分布生成覆盖导向初始个体，使部分初始 UAV 控制序列从搜索开始阶段便具有靠近任务节点区域并保持覆盖的趋势，从而改善初始种群质量，减少低覆盖区域的无效搜索。

提出覆盖状态感知 L-SHADE 算法。本文构造覆盖状态指标，并根据种群覆盖状态动态调节 L-SHADE 的搜索参数，使算法能够在覆盖构造、覆盖稳定、性能精修和覆盖恢复阶段采取不同搜索行为，从而增强算法对动态覆盖路由问题的适应能力。
开展仿真实验与结果分析。本文在固定环境下对 L-SHADE、CFI-L-SHADE 和 CSA-L-SHADE 进行多次独立运行实验，从综合目标值、覆盖率、覆盖违约、连通违约、吞吐量、队列负载和能耗等指标对算法性能进行比较，并结合收敛曲线和结果分布分析算法改进效果。
== 本文组织结构
本文共分为六章，各章内容安排如下。

第 1 章为绪论，介绍研究背景与意义、国内外研究现状、本文主要研究内容和论文结构。

第 2 章为相关理论与技术基础。主要介绍本文建模和算法设计所需的无人机通信网络组成、覆盖与连通表征、动态路由规划问题界定以及差分进化和 L-SHADE 基本原理。

第 3 章为异构无人机集群通信网络动态路由规划模型，构建 UAV 运动、无线通信、覆盖、队列、连通性表征、能耗和综合目标函数模型，并对问题进行形式化描述。

第 4 章为基于覆盖状态感知的 CSA-L-SHADE 路由优化算法，分析原始 L-SHADE 在本文问题中的不足，提出覆盖优先初始化和覆盖状态感知参数控制机制，并给出完整算法流程与复杂度分析。

第 5 章为仿真实验与结果分析，基于固定环境实验对 L-SHADE、CFI-L-SHADE 和 CSA-L-SHADE 进行对比，分析不同算法在综合目标、覆盖率、覆盖违约、吞吐量、队列负载和能耗等指标上的表现。

第 6 章为总结与展望，总结全文研究工作，归纳主要成果，并分析当前模型和算法存在的不足及未来改进方向。
= 相关理论与技术基础
== 无人机通信网络基本组成
本文研究的无人机通信网络由 UAV 节点、地面任务节点和 Sink 节点构成。UAV 节点负责在任务区域内移动、覆盖地面任务节点并进行数据转发；地面任务节点持续产生待回传数据；Sink 节点作为地面汇聚节点，负责接收 UAV 回传的数据。围绕该系统，主要存在三类链路：UAV–任务节点链路、UAV–UAV 链路和 UAV–Sink 链路。

UAV–任务节点链路用于描述地面任务节点是否被 UAV 覆盖以及数据能否进入 UAV 队列；UAV–UAV 链路用于描述多 UAV 之间的数据转发能力；UAV–Sink 链路用于刻画 UAV 向地面汇聚节点的直接回传能力。由于 UAV 具有移动性，其空间位置变化会直接改变链路距离，从而影响信道增益、传输速率和系统吞吐量。因此，UAV 通信网络优化必须同时考虑节点运动、链路质量和数据传输过程。

异构性是本文系统建模的重要特征。不同 UAV 在最大速度、覆盖半径、通信带宽、最大发射功率、缓存容量和初始能量等方面存在差异，这意味着 UAV 在覆盖、巡航、中继和回传任务中的适用角色并不相同。后续模型将在统一框架下刻画上述异构参数，并通过优化控制序列协调不同 UAV 的动态行为。

== 覆盖与连通表征
覆盖表征用于描述地面任务节点是否能够被 UAV 有效服务。常见覆盖判定方式包括基于几何距离的覆盖半径模型和基于信道质量的覆盖判定模型。本文采用距离阈值型覆盖模型，即当 UAV 与地面任务节点之间的距离不超过该 UAV 的覆盖半径时，认为该任务节点被该 UAV 覆盖。基于该覆盖关系，可以进一步统计任务周期内的平均覆盖率和覆盖违约程度。

传统连通性通常指图论意义上的节点可达性，即 UAV 网络是否形成连通拓扑。但对于本文所研究的动态通信任务，仅有拓扑可达并不足以说明数据能够稳定回传。若 UAV–Sink 链路速率不足或 UAV 队列持续积压，即使网络拓扑上存在路径，系统仍可能无法满足数据回传需求。因此，本文更关注面向 Sink 的功能性回传能力。

功能性回传能力是指系统能否以足够速率将 UAV 队列中的数据泄放至 Sink 节点。该能力由 UAV–UAV 链路、UAV–Sink 链路、链路传输速率和系统队列状态共同决定。后续模型中，本文将通过 Sink 汇聚速率、系统队列负载和回传能力违约指标描述这一过程。与纯图论连通相比，该表征方式更适合刻画动态 UAV 通信网络中的实际数据传输能力。
== 动态路由规划问题界定
在传统通信网络中，路由通常指数据包在网络节点之间选择转发路径或下一跳的过程。但在 UAV 通信网络中，节点位置随时间变化，UAV 的运动轨迹会直接影响链路质量、覆盖关系和数据回传能力。因此，UAV 动态路由规划不仅与网络层转发有关，也与 UAV 的速度、航向、发射功率和任务调度密切相关。

本文所称“动态路由规划”并非传统协议层的显式下一跳选择或路由表优化，而是规划层面的 UAV 控制序列优化。具体而言，本文通过优化 UAV 在不同控制段内的速度、航向角和发射功率，改变 UAV 的空间分布、覆盖关系和链路速率，使系统在整个任务周期内形成较好的覆盖服务能力和面向 Sink 的数据回传能力。

这一问题可以理解为“轨迹控制—覆盖服务—链路回传—队列稳定”的联合优化过程。UAV 的运动控制决定覆盖关系和链路距离，覆盖关系决定地面任务数据是否进入 UAV 队列，链路速率决定队列能否及时泄放，队列状态又影响系统的整体性能评价。因此，动态路由规划的核心不在于单独寻找一条固定路径，而在于通过连续控制变量协调 UAV 网络在时域上的服务与回传能力。

基于上述界定，本文将 UAV 分段控制序列作为优化变量。每个候选解包含所有 UAV 在多个控制段内的速度、航向角和发射功率。通过完整动态仿真，可以计算该控制序列对应的覆盖率、吞吐量、队列负载、能耗和违约指标。由此，动态路由规划问题被转化为高维连续优化问题，为引入 L-SHADE 及其改进算法提供了基础。
== 差分进化算法与 L-SHADE
差分进化算法（Differential Evolution, DE）是一类基于种群的随机优化算法@ZNXT201704001，主要用于连续变量优化问题。其基本思想是利用种群个体之间的差分向量生成变异个体，并通过交叉和选择操作不断更新种群。由于 DE 不依赖目标函数梯度信息，因此适合处理非线性、不可导和黑箱优化问题。

标准 DE 的性能对缩放因子 F、交叉概率 CR 和种群规模等参数较为敏感。较大的 F 有利于扩大搜索范围，但可能导致搜索不稳定；较小的 F 有利于局部开发，但可能降低跳出局部最优的能力；较大的 CR 会使更多维度参与更新，而较小的 CR 则使搜索更加保守。因此，参数自适应是 DE 改进研究中的重要方向。

SHADE@6900380 算法在 DE 基础上引入成功历史参数记忆机制，通过记录成功试验个体所使用的 F 和 CR，为后续参数采样提供经验依据。L-SHADE@LI2022350 进一步在线性种群规模缩减方面对 SHADE 进行改进，使算法在搜索前期保持较大种群规模以增强多样性，在搜索后期逐步缩小种群规模以提高收敛效率。因此，L-SHADE 在高维连续优化问题中具有较好的适用性。

对于本文问题而言，L-SHADE 适合作为基础优化框架，原因在于 UAV 动态路由规划可被编码为高维连续控制向量，且目标函数需要通过完整仿真计算，难以获得解析梯度。但原始 L-SHADE 的初始化、变异和参数控制均是通用机制，并不直接感知 UAV 覆盖状态。因此，若要将其有效用于动态覆盖路由问题，需要进一步引入问题结构信息，使算法能够根据覆盖状态调整搜索行为。

= 异构无人机集群通信网络动态路由规划模型
== 系统场景与基本假设

本文考虑固定飞行高度下的二维动态应急通信场景。任务区域为边长 $L$ 的正方形区域 $Omega = [0, L] times [0, L]$，默认 $L = 1000 "m"$。区域内包含 $K$ 个位置固定的地面任务节点、$N$ 架异构 UAV 和一个固定 Sink 节点 $s$。地面任务节点持续产生待传输数据，不区分业务优先级；Sink 默认位于任务区域中心，仅接收汇聚数据。

系统由 UAV 节点、地面任务节点和 Sink 节点构成。UAV 覆盖地面节点、采集任务数据，并通过 UAV-UAV 链路和 UAV-Sink 链路完成多跳数据汇聚。不同 UAV 在最大飞行速度、覆盖半径、通信带宽、最大发射功率、缓存容量和能量容量等参数上存在差异，分别对应飞行能力、覆盖能力、通信能力、存储能力和续航能力的异构性。

系统运行时间离散为 $T$ 个时隙，记为 $t = 0, 1, dots, T - 1$，每个时隙长度为 $Delta t$。UAV 在任务过程中保持固定高度，仅在二维平面内运动。每架 UAV 的控制量包括飞行速度、期望航向角和发射功率；若采用分段常量控制，则同一控制段内控制量保持不变。UAV 运动需满足最大速度、最大加速度、最大转角、最大角速度、最小转弯半径和最小安全距离等约束。

当某地面任务节点位于 UAV 有效覆盖范围内时，其产生的数据进入该 UAV 缓存队列；随后数据经 UAV 间链路逐跳转发，并最终通过 UAV-Sink 链路到达 Sink。因此，本文所称动态路由规划是指在 UAV 位置变化、链路状态变化和队列状态演化条件下，对 UAV 运动控制与传输资源进行联合规划，形成面向 Sink 的多跳数据传输过程。

本文作如下基本假设：

+ 任务区域为二维正方形区域，地面任务节点位置固定；
+ 各地面任务节点数据产生率相同，不考虑业务优先级差异；
+ UAV 保持固定飞行高度，仅在二维平面内运动；
+ UAV 与地面任务节点之间为视距通信，信道模型采用距离衰减形式；
+ 每架 UAV 具有有限缓存，未转发数据暂存于本地队列；
+ Sink 为固定地面汇聚节点，仅接收数据，不参与转发；
+ UAV 能耗由飞行能耗和通信发射能耗组成；
+ UAV 之间保持不小于给定安全距离；
+ 任务区域用于生成地面节点、Sink 和 UAV 初始位置，UAV 可行行为由运动学、通信、能量和安全距离约束共同限定。

== 核心符号与变量定义

为避免正文符号表过长，本文仅在本节保留支撑后续模型构建、算法编码和复杂度分析的核心符号。完整参数、中间变量及派生量说明见附录 A。

=== 规模参数与索引

#table(
  columns: (auto, 1fr, auto),
  table.header([符号], [含义], [单位]),
  [$cal(U) = brace.l 1, 2, dots, N brace.r$], [UAV 节点集合], [-],
  [$cal(K) = brace.l 1, 2, dots, K brace.r$], [地面任务节点集合], [-],
  [$s$], [Sink 节点], [-],
  [$cal(V) = cal(U) union brace.l s brace.r$], [通信节点集合], [-],
  [$cal(T) = brace.l 0, 1, dots, T - 1 brace.r$], [离散时隙集合], [-],
  [$cal(G) = brace.l 0, 1, dots, G - 1 brace.r$], [控制段集合], [-],
  [$N$], [UAV 数量], [-],
  [$K$], [地面任务节点数量], [-],
  [$T$], [总时隙数], [-],
  [$L_c$], [单个控制段包含的时隙数], [-],
  [$G$], [控制段总数], [-],
  [$D$], [单个候选解维度], [-],
  [$i, j$], [UAV 或通信节点索引], [-],
  [$k$], [地面任务节点索引], [-],
  [$t$], [时隙索引], [-],
  [$g$], [控制段索引], [-],
)

控制段总数为

$
G = ceil(T / L_c).
$

每架 UAV 在每个控制段内包含期望速度、期望航向角和发射功率 3 个控制量，因此单个候选解维度为

$
D = 3 N G.
$

=== 场景输入与优化变量

#table(
  columns: (auto, 1fr, auto),
  table.header([符号], [含义], [单位]),
  [$L$], [正方形任务区域边长], [m],
  [$Delta t$], [单个时隙长度], [s],
  [$bold(q)_k = [x_k^"g", y_k^"g"]^top$], [地面任务节点 $k$ 的位置], [m],
  [$bold(p)_s = [x_s, y_s]^top$], [Sink 节点位置], [m],
  [$bold(p)_i(0)$], [UAV $i$ 的初始位置], [m],
)

本文以控制段为单位优化 UAV 控制量。对于任意 $i in cal(U)$ 和 $g in cal(G)$，定义

$
bold(u)_i(g) =
[
  hat(v)_i(g),
  hat(theta)_i(g),
  P_i^"tx"(g)
].
$

其中 $hat(v)_i(g)$ 为期望飞行速度，$hat(theta)_i(g)$ 为期望航向角，$P_i^"tx"(g)$ 为发射功率。整体优化变量为

$
bold(U) =
brace.l
  bold(u)_i(g)
  mid
  i in cal(U),
  g in cal(G)
brace.r.
$

控制变量满足

$
0 <= hat(v)_i(g) <= v_i^"max",
$

$
0 <= hat(theta)_i(g) < 2 pi,
$

$
0 <= P_i^"tx"(g) <= P_i^"max".
$

=== 关键模型参数

#table(
  columns: (auto, 1fr, auto),
  table.header([符号], [含义], [单位]),
  [$v_i^"max"$], [UAV $i$ 的最大飞行速度], [m/s],
  [$P_i^"max"$], [UAV $i$ 的最大发射功率], [W],
  [$R_i^"cov"$], [UAV $i$ 的覆盖半径], [m],
  [$B_i$], [UAV $i$ 的通信带宽], [Hz],
  [$Q_i^"max"$], [UAV $i$ 的最大缓存容量], [bit],
  [$E_i^"max"$], [UAV $i$ 的最大能量容量], [J],
  [$d_"safe"$], [UAV 间最小安全距离], [m],
  [$epsilon_"con"$], [最小数据泄放比例系数], [-],
  [$lambda_0, lambda_1, lambda_2, lambda_3, lambda_4$], [综合目标函数权重], [-],
)

其中，不同 UAV 可具有不同的 $v_i^"max"$、$P_i^"max"$、$R_i^"cov"$、$B_i$、$Q_i^"max"$ 和 $E_i^"max"$，用于描述 UAV 集群的异构性。

=== 主要状态量与评价输出

#table(
  columns: (auto, 1fr, auto),
  table.header([符号], [含义], [单位]),
  [$bold(p)_i(t)$], [UAV $i$ 在时隙 $t$ 的二维位置], [m],
  [$v_i(t)$], [UAV $i$ 在时隙 $t$ 的实际执行速度], [m/s],
  [$phi_i(t)$], [UAV $i$ 在时隙 $t$ 的实际执行航向角], [rad],
  [$Q_i(t)$], [UAV $i$ 在时隙 $t$ 的缓存队列长度], [bit],
  [$E_i(t)$], [UAV $i$ 在时隙 $t$ 的剩余能量], [J],
  [$R_"norm"$], [归一化系统吞吐量], [-],
  [$Q_"norm"$], [归一化队列负载], [-],
  [$E_"norm"$], [归一化能耗指标], [-],
  [$V_"cov"^"norm"$], [归一化覆盖违约指标], [-],
  [$V_"con"^"norm"$], [归一化连通性违约指标], [-],
  [$J_"perf"$], [通信性能评价项], [-],
  [$J_"total"$], [综合目标函数值], [-],
)

上述评价输出量由完整任务周期仿真得到，用于个体排序、层级判优、覆盖状态估计和最优解输出。

== 无人机运动模型

在固定飞行高度条件下，UAV 运动被建模为二维平面内的离散时间更新过程。对于任意 UAV $i in cal(U)$，其在时隙 $t$ 的运动状态表示为

$
bold(s)_i(t)
=
[
  x_i(t),
  y_i(t),
  v_i(t),
  phi_i(t)
]^top.
$

其中，$bold(p)_i(t) = [x_i(t), y_i(t)]^top$ 为二维位置，$v_i(t)$ 为实际执行速度，$phi_i(t)$ 为实际执行航向角。初始速度和初始航向设为

$
v_i(0) = 0,
quad
phi_i(0) = 0.
$

系统以控制段为单位给出期望控制量。对于任意 $t in cal(T)_g$，UAV $i$ 接收控制段 $g$ 内的期望速度 $hat(v)_i(g)$ 和期望航向角 $hat(theta)_i(g)$。实际执行速度由期望速度、最大速度和最大切向加速度共同确定：

$
v_i(t)
=
op("clip")(
  hat(v)_i(g),
  max(0, v_i(t - 1) - a^"max" Delta t),
  min(v_i^"max", v_i(t - 1) + a^"max" Delta t)
).
$

并满足

$
0 <= v_i(t) <= v_i^"max".
$

其中，$op("clip")(x, l, u)$ 表示将 $x$ 截断至区间 $[l, u]$。

控制变量 $hat(theta)_i(g)$ 表示 UAV $i$ 在控制段 $g$ 内的期望绝对航向角。期望航向与上一时隙实际航向之间的最短角差定义为

$
delta_i(t)
=
op("wrap")_([-pi, pi])
(
  hat(theta)_i(g) - phi_i(t - 1)
).
$

单时隙最大允许航向变化量分两种情况定义。当 $v_i(t) > 0$ 时，

$
Delta phi_i^"max"(t)
=
min(
  Delta theta^"max",
  omega^"max" Delta t,
  (v_i(t) Delta t) / R^"min"
).
$

当 $v_i(t) = 0$ 时，

$
Delta phi_i^"max"(t)
=
min(
  Delta theta^"max",
  omega^"max" Delta t
).
$

实际执行转角为

$
delta_i^"exe"(t)
=
op("clip")(
  delta_i(t),
  -Delta phi_i^"max"(t),
  Delta phi_i^"max"(t)
).
$

实际执行航向角更新为

$
phi_i(t)
=
op("wrap")_([0, 2 pi))
(
  phi_i(t - 1) + delta_i^"exe"(t)
).
$

其中，$op("wrap")_([-pi, pi]) (dot)$ 和 $op("wrap")_([0, 2 pi)) (dot)$ 分别表示角度区间映射函数。

获得实际执行速度 $v_i(t)$ 和实际执行航向 $phi_i(t)$ 后，UAV 的候选位置按离散运动方程更新：

$
tilde(bold(p))_i(t + 1)
=
bold(p)_i(t)
+
v_i(t) Delta t
[
  cos phi_i(t),
  sin phi_i(t)
]^top.
$

等价地，

$
tilde(x)_i(t + 1)
=
x_i(t) + v_i(t) Delta t cos phi_i(t),
$

$
tilde(y)_i(t + 1)
=
y_i(t) + v_i(t) Delta t sin phi_i(t).
$

为避免 UAV 间距离过近，任意两架 UAV 应满足最小安全距离约束：

$
norm(bold(p)_i(t) - bold(p)_j(t))_2
>=
d_"safe",
quad
forall i, j in cal(U),
i != j.
$

在位置更新时，系统先计算候选位置 $tilde(bold(p))_i(t + 1)$。若存在 $j != i$，使得

$
norm(tilde(bold(p))_i(t + 1) - tilde(bold(p))_j(t + 1))_2
<
d_"safe",
$

则取消 UAV $i$ 当前时隙的位移更新，并将其实际执行速度置零：

$
bold(p)_i(t + 1) = bold(p)_i(t),
quad
v_i(t) = 0.
$

否则，接受候选位置：

$
bold(p)_i(t + 1) = tilde(bold(p))_i(t + 1).
$

综上，UAV 的运动状态由期望控制量、速度约束、航向约束、位置更新和安全距离修正共同确定。该运动模型输出的 $bold(p)_i(t)$、$v_i(t)$ 和 $phi_i(t)$ 将作为后续通信距离、覆盖关系和飞行能耗计算的输入。

== 无线通信模型

本节讨论 UAV-UAV 转发链路和 UAV-Sink 汇聚链路，统一记接收节点为 $j in cal(V) = cal(U) union brace.l s brace.r$。假设链路满足视距通信，忽略同频干扰、多径衰落及 LoS/NLoS 随机切换。对于任意发射 UAV $i in cal(U)$ 和接收节点 $j in cal(V)$，$i != j$，当 $j in cal(U)$ 时，接收节点位置为 $bold(p)_j(t)$；当 $j = s$ 时，接收节点位置为 $bold(p)_s$。

则链路距离为

$
d_(i j)(t)
=
norm(bold(p)_i(t) - bold(p)_j(t))_2,
quad
i in cal(U),
j in cal(V),
i != j.
$

链路信道增益采用距离平方衰减模型：

$
g_(i j)(t)
=
beta_0 / (d_(i j)^2(t) + delta),
quad
i in cal(U),
j in cal(V),
i != j.
$

其中，$beta_0$ 为参考距离增益系数，$delta$ 为信道增益修正项。

链路 $i -> j$ 在时隙 $t$ 的信噪比为

$
gamma_(i j)(t)
=
(P_i^"tx"(t) g_(i j)(t)) / sigma^2,
$

其中，$P_i^"tx"(t)$ 为 UAV $i$ 的发射功率，$sigma^2$ 为噪声功率。相应的可达传输速率为

$
r_(i j)(t)
=
B_i log_2(1 + gamma_(i j)(t)),
quad
i in cal(U),
j in cal(V),
i != j.
$

等价地，

$
r_(i j)(t)
=
B_i log_2(
  1 + (P_i^"tx"(t) g_(i j)(t)) / sigma^2
).
$

其中，$B_i$ 为发送 UAV $i$ 的通信带宽。对于 UAV 自身到自身的无效链路，规定

$
r_(i i)(t) = 0,
quad
i in cal(U).
$

为统一表示各时隙内的链路传输能力，定义速率矩阵

$
bold(R)(t)
=
[r_(i j)(t)]_(i in cal(U), j in cal(V))
in
RR^(N times (N + 1)).
$

其中，$j in cal(U)$ 对应 UAV-UAV 转发速率，$j = s$ 对应 UAV-Sink 汇聚速率。后续队列模型基于 $bold(R)(t)$ 计算 UAV 间等效转发流量和 Sink 侧数据泄放能力。

== 地面节点覆盖模型

为刻画 UAV 对地面任务节点的数据采集条件，定义时隙 $t$ 内 UAV $i in cal(U)$ 与地面任务节点 $k in cal(K)$ 的二维距离为

$
d_(i k)^"g"(t)
=
norm(bold(p)_i(t) - bold(q)_k)_2.
$

考虑 UAV 覆盖半径 $R_i^"cov"$，二元覆盖指示量定义为

$
c_(i k)(t)
=
bb(I)(
  d_(i k)^"g"(t) <= R_i^"cov"
),
quad
i in cal(U),
k in cal(K),
t in cal(T),
$

其中，$bb(I)(dot)$ 为示性函数。任务节点 $k$ 在时隙 $t$ 的覆盖状态为

$
z_k(t)
=
min(
  1,
  sum_(i in cal(U)) c_(i k)(t)
).
$

据此，系统在时隙 $t$ 的覆盖率和整个任务周期内的平均覆盖率分别为

$
rho_"cov"(t)
=
1 / K
sum_(k in cal(K)) z_k(t),
$

$
rho_"cov"
=
1 / T
sum_(t in cal(T)) rho_"cov"(t)
=
1 / (T K)
sum_(t in cal(T))
sum_(k in cal(K))
z_k(t).
$

覆盖不足程度由覆盖违约量表示。时隙 $t$ 的覆盖违约量定义为

$
V_"cov"(t)
=
sum_(k in cal(K))
(1 - z_k(t)).
$

累计覆盖违约量为

$
V_"cov"^"sum"
=
sum_(t in cal(T))
V_"cov"(t).
$

归一化覆盖违约指标为

$
V_"cov"^"norm"
=
V_"cov"^"sum" / (T K).
$

在上述定义下，有

$
V_"cov"^"norm"
=
1 - rho_"cov".
$

不将任意时隙全覆盖作为硬约束，而以 $V_"cov"^"norm"$ 量化覆盖不足。该指标将进入综合目标函数，并作为第 4 章算法层级判优和覆盖状态感知参数控制的核心依据。

== 数据队列与连通性表征模型

为刻画数据流转过程，本节基于链路速率建立 UAV 缓存队列模型。系统连通性定义为 UAV 网络将缓存数据有效回传至 Sink 的功能性能力，而非静态拓扑全连通属性。

*数据到达量。* 地面任务节点 $k$ 在单个时隙内产生的数据量为

$
a_k = lambda_k Delta t.
$

若任务节点 $k$ 处于 UAV $i$ 的覆盖范围内，即 $c_(i k)(t) = 1$，则其数据进入 UAV $i$ 的缓存队列。UAV $i$ 在时隙 $t$ 的地面数据到达量为

$
A_i(t)
=
sum_(k in cal(K)) c_(i k)(t) a_k
=
sum_(k in cal(K)) c_(i k)(t) lambda_k Delta t.
$

本文采用并行采集假设，即同一任务节点被多架 UAV 覆盖时，其数据可分别进入对应 UAV 的缓存队列。

*等效转发流量。* 基于第 3.4 节得到的链路速率 $r_(i j)(t)$，定义 UAV $i$ 在时隙 $t$ 的等效转发流入速率和流出速率分别为

$
r_i^"in"(t)
=
sum_(j in cal(U), j != i)
r_(j i)(t),
$

$
r_i^"out"(t)
=
sum_(j in cal(U), j != i)
r_(i j)(t)
+
r_(i s)(t).
$

对应的数据流入量和流出量为

$
F_i^"in"(t) = r_i^"in"(t) Delta t,
quad
F_i^"out"(t) = r_i^"out"(t) Delta t.
$

*队列更新。* 设 $Q_i(t)$ 为 UAV $i$ 在时隙 $t$ 的缓存队列长度，初始队列为

$
Q_i(0) = 0.
$

则队列状态更新为

$
Q_i(t + 1)
=
op("clip")(
  Q_i(t)
  +
  A_i(t)
  +
  (r_i^"in"(t) - r_i^"out"(t)) Delta t,
  0,
  Q_i^"max"
),
$

其中 $Q_i^"max"$ 为 UAV $i$ 的最大缓存容量。系统总队列长度定义为

$
Q_"sys"(t)
=
sum_(i in cal(U))
Q_i(t).
$

*连通性表征。* Sink 侧的总汇聚速率为

$
R_s(t)
=
sum_(i in cal(U))
r_(i s)(t).
$

单时隙内的等效数据泄放量为

$
D_s(t)
=
R_s(t) Delta t.
$

设 $epsilon_"con"$ 为最小数据泄放比例系数，则时隙 $t$ 的功能性连通约束可表示为

$
D_s(t)
>=
epsilon_"con" Q_"sys"(t).
$

相应地，定义连通性违约量为

$
V_"con"(t)
=
max(
  0,
  epsilon_"con" Q_"sys"(t) - D_s(t)
).
$

整个任务周期内的累计连通性违约量为

$
V_"con"^"sum"
=
sum_(t in cal(T))
V_"con"(t).
$

记系统总缓存容量为

$
Q_"max"
=
sum_(i in cal(U))
Q_i^"max".
$

则归一化连通性违约指标为

$
V_"con"^"norm"
=
V_"con"^"sum" / (T epsilon_"con" Q_"max").
$

综上，数据队列模型由覆盖驱动的数据到达、链路速率驱动的等效转发和 Sink 数据泄放能力共同构成。其中，$Q_i(t)$ 用于刻画 UAV 缓存状态，$Q_"sys"(t)$ 用于刻画系统负载，$V_"con"^"norm"$ 用于后续综合目标函数与算法层级判优。

== 无人机能量消耗模型

UAV 的能量消耗由飞行能耗和通信发射能耗构成。对于任意 UAV $i in cal(U)$，其在时隙 $t$ 的飞行功率定义为

$
P_i^"fly"(t)
=
P_i^"hover"
+
c_(2, i)^"fly" v_i^2(t)
+
c_(3, i)^"fly" v_i^3(t),
$

其中，$P_i^"hover"$ 为悬停功率，$c_(2, i)^"fly"$ 和 $c_(3, i)^"fly"$ 分别为速度平方项和速度立方项飞行功率系数。对应的飞行能耗为

$
E_i^"fly"(t)
=
P_i^"fly"(t) Delta t.
$

通信能耗仅考虑发射功率消耗，表示为

$
E_i^"tx"(t)
=
P_i^"tx"(t) Delta t.
$

因此，UAV $i$ 在时隙 $t$ 的总功率为

$
P_i^"total"(t)
=
P_i^"fly"(t)
+
P_i^"tx"(t),
$

即

$
P_i^"total"(t)
=
P_i^"hover"
+
c_(2, i)^"fly" v_i^2(t)
+
c_(3, i)^"fly" v_i^3(t)
+
P_i^"tx"(t).
$

设 $E_i(t)$ 为 UAV $i$ 在时隙 $t$ 开始时的剩余能量，则能量状态更新为

$
E_i(t + 1)
=
op("clip")(
  E_i(t) - P_i^"total"(t) Delta t,
  0,
  E_i^"max"
),
$

初始能量满足

$
E_i(0) = E_i^"max".
$

系统在时隙 $t$ 内的总能耗为

$
E_"sys"(t)
=
sum_(i in cal(U))
P_i^"total"(t) Delta t.
$

整个任务周期内的累计系统能耗为

$
E_"sum"
=
sum_(t in cal(T))
E_"sys"(t).
$

记系统总初始能量容量为

$
E_"max"
=
sum_(i in cal(U))
E_i^"max".
$

归一化能耗指标定义为

$
E_"norm"
=
E_"sum" / (T E_"max").
$

综上，能量模型由飞行功率、通信发射功率和剩余能量演化共同构成。其中，$E_"norm"$ 将作为综合目标函数中的能耗评价项。

= 基于覆盖状态感知的 CSA-L-SHADE 路由优化算法
== L-SHADE 在本文问题中的求解框架

L-SHADE 是一种基于差分进化的自适应连续优化算法，其核心机制包括 current-to-pbest/1 变异、二项交叉、成功历史参数自适应、外部档案和线性种群规模缩减。第 2 章已对其基本原理进行了介绍，本节重点说明 L-SHADE 在本文异构 UAV 动态路由规划问题中的编码、评估与选择方式。

在本文问题中，一个个体表示完整任务周期内所有 UAV 的分段控制序列。对于 UAV $i$ 和控制段 $g$，其控制向量为

$
bold(u)_i(g)
=
[
  hat(v)_i(g),
  hat(theta)_i(g),
  P_i^"tx"(g)
]^top.
$

因此，候选解可表示为由所有 UAV、所有控制段控制量拼接而成的连续向量：

$
bold(x) in RR^D,
quad
D = 3 N G.
$

其中，$N$ 为 UAV 数量，$G$ 为控制段数量。该编码方式将 UAV 的轨迹控制与发射功率控制统一到连续优化空间中。

对于任意候选解 $bold(x)$，算法首先将其解码为完整时域动作序列，并输入第 3 章建立的动态仿真模型。仿真结束后得到个体评价向量：

$
bold(C)(bold(x))
=
(
  V_"cov"^"norm",
  V_"con"^"norm",
  J_"perf",
  J_"total"
).
$

与普通 L-SHADE 直接比较单一目标值不同，本文采用覆盖与回传能力优先的层级判优规则：优先比较 $V_"cov"^"norm"$，其次比较 $V_"con"^"norm"$，在二者相近时再比较 $J_"perf"$ 和 $J_"total"$。该规则使算法搜索过程优先压低覆盖违约和回传能力违约，再进一步优化吞吐、队列和能耗组成的综合性能。

在变异阶段，L-SHADE 采用 current-to-pbest/1 策略生成变异向量：

$
bold(v)_i
=
bold(x)_i
+
F_i (bold(x)_"pbest" - bold(x)_i)
+
F_i (bold(x)_(r_1) - bold(x)_(r_2)).
$

其中，$bold(x)_"pbest"$ 从当前种群前 $p$ 比例优秀个体中随机选取，$F_i$ 为缩放因子，$bold(x)_(r_2)$ 可来自当前种群或外部档案。随后，变异向量经二项交叉形成试验个体，并按第 3 章定义的速度、航向角和发射功率范围进行边界修复。

L-SHADE 通过成功历史记忆机制自适应采样 $F_i$ 和 $C R_i$，并通过线性种群规模缩减在搜索前期保持多样性、后期增强收敛能力。本文后续提出的 CSA 机制不替代这一成功历史自适应过程，而是在其采样结果基础上，根据种群覆盖状态对 $p$、$F$ 和 $C R$ 进行状态相关控制。由此，L-SHADE 构成 CSA-L-SHADE 的基础连续搜索框架。

== 原始 L-SHADE 的适配性挑战

原始 L-SHADE 具有较强的连续优化能力，但本文研究的异构 UAV 动态覆盖路由问题并非一般黑箱连续优化问题。候选解由多架 UAV 在多个控制段内的速度、航向角和发射功率组成，目标值则由 UAV 轨迹、地面覆盖、链路速率、队列演化和能量消耗共同决定。其中，覆盖状态不仅是综合目标函数中的惩罚项，更是后续数据到达、队列更新和 Sink 回传能力评价的基础。因此，若算法不能在搜索过程中有效形成和维持覆盖结构，后续吞吐量、队列负载和能耗优化也会受到限制。

首先，原始 L-SHADE 通常采用随机初始化，难以在任务早期形成高质量覆盖结构。本文中一个个体对应 $D = 3 N G$ 维 UAV 分段控制向量，随机生成的速度、航向角和发射功率序列不包含任务节点空间分布信息，可能导致初始 UAV 轨迹偏离任务节点密集区域。当初始种群整体覆盖水平较低时，算法需要消耗较多函数评估次数才能逐步修正轨迹方向，容易造成搜索前期覆盖违约较高。这一问题体现为搜索入口与有效覆盖区域之间存在结构性偏差。

其次，L-SHADE 的差分变异本质上是一种通用向量扰动，搜索方向主要由种群中个体差分和 pbest 引导项决定。然而，在本文问题中，控制变量与性能指标之间存在明确的物理耦合关系：速度和航向角影响 UAV 位置，位置决定覆盖关系和链路距离，发射功率同时影响传输速率和通信能耗。原始差分扰动并不显式感知任务节点位置、覆盖半径和 Sink 回传方向，因此难以稳定产生符合覆盖路由结构的搜索行为。

再次，覆盖判定具有明显的阈值非光滑性。任务节点是否被覆盖由 UAV 与任务节点之间的距离是否小于覆盖半径决定，即

$
c_(i k)(t)
=
bb(I)(
  d_(i k)^"g"(t) <= R_i^"cov"
).
$

因此，连续控制变量的微小变化可能导致覆盖状态发生跳变，也可能因未跨越覆盖边界而对覆盖指标没有影响。这种非光滑特征会削弱 L-SHADE 成功历史参数反馈的稳定性，使 $F$ 和 $C R$ 的历史经验难以准确反映覆盖结构变化。

最后，原始 L-SHADE 的参数控制不区分覆盖状态。对于本文问题，搜索早期需要较强探索以构造覆盖结构；覆盖初步形成后，需要降低破坏性扰动以维持覆盖；覆盖稳定后，才适合进一步优化吞吐量、队列负载和能耗；若覆盖发生退化，则需要重新增强覆盖恢复能力。原始 L-SHADE 主要依赖历史成功参数进行自适应，缺少对上述覆盖阶段的显式判断，容易出现覆盖尚未稳定时过早转向性能优化，或覆盖退化后缺少恢复机制的问题。

综上，原始 L-SHADE 适合作为本文问题的基础连续优化框架，但其随机初始化、通用差分扰动和覆盖状态无关的参数控制难以充分适配动态覆盖路由问题。基于此，本文在 L-SHADE 基础上引入两类结构化改进：一是覆盖优先初始化机制，用于改善初始种群的覆盖质量；二是覆盖状态感知参数控制机制，用于根据种群覆盖状态动态调节 $p$、$F$ 和 $C R$。前者解决搜索入口问题，后者解决搜索过程中的覆盖状态适配问题。

== 覆盖优先初始化机制

针对原始 L-SHADE 随机初始化难以在任务早期形成有效覆盖结构的问题，本文在 CSA-L-SHADE 中引入覆盖优先初始化机制（Coverage-First Initialization, CFI）。该机制利用地面任务节点的空间分布构造部分覆盖导向初始个体，使初始种群中包含更接近覆盖可行区域的控制序列。CFI 仅作用于种群初始化阶段，不改变 L-SHADE 后续的变异、交叉、选择、外部档案更新和成功历史记忆更新过程。

设地面任务节点集合为 $cal(K) = brace.l 1, 2, dots, K brace.r$，任务节点 $k$ 的位置为 $bold(q)_k$。对于每个 CFI 个体，首先从任务节点中选取

$
N_c = min(N, K)
$

个覆盖目标点。为兼顾任务节点分布中心和空间分散性，本文采用“质心起点-最远点扩展”的目标点选择策略。首先计算任务节点位置质心：

$
overline(bold(q))
=
1 / K
sum_(k in cal(K))
bold(q)_k.
$

选取距离质心最近的任务节点作为初始目标点：

$
k_1
=
arg min_(k in cal(K))
norm(bold(q)_k - overline(bold(q)))_2.
$

记当前已选目标集合为 $cal(C)$。在后续选择过程中，对于尚未被选中的任务节点，计算其到已选目标集合的最小距离，并选取该距离最大的节点作为新的覆盖目标：

$
k^*
=
arg max_(k in cal(K) without cal(C))
min_(m in cal(C))
norm(bold(q)_k - bold(q)_m)_2.
$

重复上述过程，直至满足

$
|cal(C)| = N_c.
$

由此得到的目标集合 $cal(C)$ 能够在任务区域内形成相对分散的覆盖引导点。

在目标分配阶段，CFI 按 UAV 顺序进行贪心匹配。设 $cal(C)_i$ 表示分配 UAV $i$ 时尚未使用的目标集合。若 $cal(C)_i != emptyset$，则 UAV $i$ 被分配至距离其初始位置最近的未使用目标：

$
k(i)
=
arg min_(k in cal(C)_i)
norm(bold(p)_i(0) - bold(q)_k)_2.
$

若所有目标均已被分配，则允许目标复用，此时有

$
k(i)
=
arg min_(k in cal(C))
norm(bold(p)_i(0) - bold(q)_k)_2.
$

因此，当 $N > K$ 时，前 $K$ 架 UAV 可优先对应不同任务节点，剩余 UAV 则可复用已有覆盖目标。

为避免 CFI 个体在同一目标附近完全重合，本文在分配目标周围引入随机扰动。UAV $i$ 的扰动目标位置定义为

$
tilde(bold(q))_i
=
bold(q)_(k(i))
+
bold(xi)_i,
$

其中

$
bold(xi)_i
∼
cal(N)(
  bold(0),
  sigma_i^2 bold(I)_2
).
$

扰动尺度 $sigma_i$ 与 UAV 覆盖半径和任务节点空间分布有关。当任务节点数量为 1 时，取

$
sigma_i
=
0.15 R_i^"cov".
$

否则，取

$
sigma_i
=
min(
  max(
    0.05 R_i^"cov",
    0.08 norm(
      op("std")(
        brace.l bold(q)_k brace.r_(k in cal(K))
      )
    )_2
  ),
  0.25 R_i^"cov"
).
$

其中，$op("std")(brace.l bold(q)_k brace.r_(k in cal(K)))$ 表示地面任务节点坐标在二维方向上的标准差向量。

在获得扰动目标点后，CFI 为每架 UAV 构造分段控制序列。对于 UAV $i$ 和控制段 $g in cal(G)$，控制向量仍定义为

$
bold(u)_i(g)
=
[
  hat(v)_i(g),
  hat(theta)_i(g),
  P_i^"tx"(g)
]^top.
$

设控制段总数为 $G$，接近阶段的基准长度为

$
G_"tr"
=
ceil(0.25 G).
$

令 $overline(bold(p))_i(g) = [overline(x)_i(g), overline(y)_i(g)]^top$ 表示生成第 $g$ 个控制段时的 UAV 估计位置。若满足

$
g < G_"tr"
$

或

$
norm(overline(bold(p))_i(g) - tilde(bold(q))_i)_2
>
0.6 R_i^"cov",
$

则 UAV $i$ 在控制段 $g$ 采用接近模式。此时，期望航向角指向扰动目标：

$
hat(theta)_i(g)
=
op("atan2")(
  tilde(y)_i - overline(y)_i(g),
  tilde(x)_i - overline(x)_i(g)
).
$

速度和发射功率分别由比例系数生成：

$
hat(v)_i(g)
=
alpha_(v, i)^"tr"(g) v_i^"max",
quad
alpha_(v, i)^"tr"(g) ~ cal(U)(0.65, 0.90),
$

$
P_i^"tx"(g)
=
alpha_(p, i)^"tr"(g) P_i^"max",
quad
alpha_(p, i)^"tr"(g) ~ cal(U)(0.65, 0.90).
$

若上述条件不满足，则 UAV $i$ 在控制段 $g$ 采用保持模式。此时期望航向角在指向目标方向的基础上加入小幅角度扰动：

$
hat(theta)_i(g)
=
op("atan2")(
  tilde(y)_i - overline(y)_i(g),
  tilde(x)_i - overline(x)_i(g)
)
+
zeta_i(g),
$

其中

$
zeta_i(g) ~ cal(U)(-0.35, 0.35).
$

保持模式下的速度和发射功率为

$
hat(v)_i(g)
=
alpha_(v, i)^"h"(g) v_i^"max",
quad
alpha_(v, i)^"h"(g) ~ cal(U)(0.05, 0.20),
$

$
P_i^"tx"(g)
=
alpha_(p, i)^"h"(g) P_i^"max",
quad
alpha_(p, i)^"h"(g) ~ cal(U)(0.45, 0.70).
$

生成后的控制变量需满足第三章定义的边界条件：

$
0 <= hat(v)_i(g) <= v_i^"max",
quad
0 <= hat(theta)_i(g) < 2 pi,
quad
0 <= P_i^"tx"(g) <= P_i^"max".
$

在仿真评估阶段，实际执行速度和航向还将进一步受到加速度、最大转角、最大角速度、最小转弯半径和安全距离修正机制的约束。

CFI 与随机初始化共同构成初始种群。设初始种群规模为 $N P_0$，覆盖优先初始化比例为 $rho_"CFI"$，则 CFI 个体数量和随机初始化个体数量分别为

$
N P_"CFI"
=
op("round")(
  rho_"CFI" N P_0
),
$

$
N P_"rand"
=
N P_0 - N P_"CFI".
$

本文实验中采用混合初始化设置：

$
N P_0 = 100,
quad
rho_"CFI" = 0.5.
$

因此，初始种群中约一半个体由 CFI 生成，其余个体采用随机初始化生成。初始化完成后，两类个体不再区别处理，均作为 L-SHADE 种群中的实数向量参与后续搜索。通过该机制，初始种群在保持随机多样性的同时获得覆盖导向结构，为后续覆盖状态感知参数控制提供更稳定的初始搜索基础。

== 覆盖状态感知参数控制机制

覆盖优先初始化改善了初始种群的覆盖质量，但在后续差分进化过程中，种群仍可能出现覆盖退化或过早转向性能优化。为此，本文在 CFI-L-SHADE 的基础上引入覆盖状态感知参数控制机制（Coverage-State-Aware Parameter Control, CSA）。该机制根据种群覆盖状态动态约束 $p$、$F$ 和 $"CR"$，使算法在覆盖构造、覆盖稳定、性能精修和覆盖恢复阶段采用不同搜索尺度。

=== 覆盖状态指标构造

设第 $ell$ 代种群为

$
cal(P)^(ell)
=
brace.l
  bold(x)_1^(ell),
  bold(x)_2^(ell),
  dots,
  bold(x)_(N P_ell)^(ell)
brace.r,
$

其中 $N P_ell$ 为第 $ell$ 代种群规模。每个个体经完整动态仿真后得到评价向量

$
bold(C)(bold(x)_i^(ell))
=
(
  V_"cov"^"norm",
  V_"con"^"norm",
  J_"perf",
  J_"total"
).
$

本文按字典序对 $bold(C)(bold(x)_i^(ell))$ 进行升序排序，即优先比较覆盖违约，其次比较连通性违约，再比较通信性能项和综合目标值。设排序后的种群为

$
op("rank")(
  cal(P)^(ell)
).
$

取前 $rho_e$ 比例个体构成精英集合：

$
N_e^(ell)
=
ceil(rho_e N P_ell),
quad
rho_e = 0.20.
$

$
cal(E)^(ell)
=
brace.l
  bold(x) in op("rank")(cal(P)^(ell))
  mid
  op("rank")(bold(x)) <= N_e^(ell)
brace.r.
$

基于精英集合，定义精英覆盖违约均值

$
g_ell
=
1 / |cal(E)^(ell)|
sum_(bold(x) in cal(E)^(ell))
V_"cov"^"norm"(bold(x)).
$

设 coverage-safe 阈值为

$
tau_"safe" = 0.10.
$

第 $ell$ 代种群中的 coverage-safe 比例定义为

$
r_ell
=
1 / (N P_ell)
sum_(i = 1)^(N P_ell)
bb(I)(
  V_"cov"^"norm"(bold(x)_i^(ell))
  <=
  tau_"safe"
).
$

进一步设状态判断窗口长度为

$
L_s = 5,
$

定义精英覆盖违约变化量为

$
Delta g_ell
=
g_(ell - L_s) - g_ell.
$

因此，CSA-L-SHADE 采用三类覆盖状态统计量

$
(
  g_ell,
  r_ell,
  Delta g_ell
)
$

刻画当前种群覆盖水平、coverage-safe 个体扩散程度及近期覆盖变化趋势。

=== 覆盖状态机

定义覆盖状态空间为

$
cal(S)
=
brace.l
  S_1, S_2, S_3, S_4
brace.r,
$

其中 $S_1$、$S_2$、$S_3$、$S_4$ 分别表示覆盖构造、覆盖稳定、性能精修和覆盖恢复状态，如表 4-1 所示。

#table(
  columns: (auto, auto, 1fr, 1fr),
  table.header([状态], [名称], [状态含义], [搜索目标]),
  [$S_1$], [Coverage Construction], [覆盖尚未建立], [快速形成覆盖结构],
  [$S_2$], [Coverage Stabilization], [覆盖初步形成但尚不稳定], [稳定并扩散覆盖结构],
  [$S_3$], [Performance Refinement], [覆盖已相对稳定], [优化吞吐、队列和能耗],
  [$S_4$], [Coverage Recovery], [覆盖发生退化], [恢复覆盖结构],
)

状态转移由 $g_ell$、$r_ell$、$Delta g_ell$ 及稳定计数器 $c_"stab"$ 共同决定。本文采用如下阈值：

$
tau_H = 0.15,
quad
tau_L = 0.10,
$

$
r_L = 0.20,
quad
r_H = 0.50,
$

$
Delta_"stable" = 0.002,
quad
H_"req" = 3,
$

$
tau_"recover" = 0.11,
quad
r_"recover" = 0.40.
$

稳定条件定义为

$
cal(A)_ell
=
[g_ell <= tau_L]
and
[r_ell >= r_H]
and
[Delta g_ell <= Delta_"stable"].
$

稳定计数器按如下规则更新：若 $cal(A)_ell$ 成立，则 $c_"stab" <- c_"stab" + 1$；否则 $c_"stab" <- 0$。

覆盖状态转移函数定义为

$
S_(ell + 1)
=
cal(F)(
  S_ell,
  g_ell,
  r_ell,
  Delta g_ell,
  c_"stab"
).
$

具体转移规则如下：

#table(
  columns: (auto, 1fr),
  table.header([转移结果], [触发条件]),
  [$S_(ell + 1) = S_2$], [$S_ell = S_1$, $g_ell <= tau_H$，且 $r_ell >= r_L$],
  [$S_(ell + 1) = S_3$], [$S_ell = S_2$，且 $c_"stab" >= H_"req"$],
  [$S_(ell + 1) = S_4$], [$S_ell = S_3$，且 $g_ell > tau_"recover"$ 或 $r_ell < r_"recover"$],
  [$S_(ell + 1) = S_2$], [$S_ell = S_4$，$g_ell <= tau_H$，且 $r_ell >= r_L$],
  [$S_(ell + 1) = S_1$], [$S_ell = S_4$，且 $g_ell > tau_H$ 或 $r_ell < r_L$],
  [$S_(ell + 1) = S_ell$], [其他情况],
)

其中，$S_4$ 仅在性能精修阶段发生覆盖退化时触发；若恢复后达到基本覆盖条件，则返回 $S_2$，否则回退至 $S_1$。

=== 状态驱动的 p/F/CR 控制

CSA-L-SHADE 保留 L-SHADE 的成功历史记忆机制。对于个体 $bold(x)_i^(ell)$，缩放因子和交叉概率首先由历史记忆采样得到：

$
F_i
∼
op("Cauchy")(
  M_F[r],
  0.1
),
$

$
"CR"_i
∼
cal(N)(
  M_"CR"[r],
  0.1
),
$

其中 $r$ 为随机选取的历史记忆索引。随后，根据当前覆盖状态 $S_ell$ 对参数进行状态相关裁剪：

$
F_i
<-
op("clip")(
  F_i,
  F_"min"(S_ell),
  F_"max"(S_ell)
).
$

$
"CR"_i
<-
op("clip")(
  "CR"_i,
  "CR"_"min"(S_ell),
  "CR"_"max"(S_ell)
).
$

同时，pbest 选择比例由当前状态确定：

$
p_ell
=
p(S_ell).
$

状态驱动参数设置如表 4-2 所示。

#table(
  columns: (1fr, auto, auto, auto, 1fr),
  table.header([覆盖状态], [$p_ell$], [$F$ 范围], [$"CR"$ 范围], [参数控制意图]),
  [$S_1$ Coverage Construction], [0.08], [$[0.50, 0.80]$], [$[0.15, 0.40]$], [加强覆盖构造],
  [$S_2$ Coverage Stabilization], [0.10], [$[0.35, 0.60]$], [$[0.10, 0.30]$], [稳定覆盖结构],
  [$S_3$ Performance Refinement], [0.15], [$[0.20, 0.50]$], [$[0.10, 0.35]$], [精修通信性能],
  [$S_4$ Coverage Recovery], [0.08], [$[0.35, 0.60]$], [$[0.10, 0.25]$], [恢复覆盖结构],
)

由此，CSA 机制并不替代 L-SHADE 的成功历史自适应，而是在其采样结果外施加覆盖状态约束。$p_ell$ 控制 pbest 候选范围，$F_i$ 控制差分变异步长，$"CR"_i$ 控制交叉强度。覆盖构造和覆盖恢复状态采用较小 $p_ell$，以强化高覆盖个体的引导；覆盖稳定和性能精修状态逐步降低搜索扰动，使算法在保持覆盖结构的基础上优化吞吐量、队列负载和能耗。

综上，覆盖状态感知参数控制机制将 $V_"cov"^"norm"$ 的种群统计量引入 L-SHADE 参数控制过程。该机制不改变第 3 章定义的目标函数，也不引入新的覆盖硬约束，而是在搜索过程中动态调整 $p$、$F$ 和 $"CR"$，从而增强算法对覆盖构造、覆盖稳定、性能精修和覆盖恢复阶段的适配能力。
== CSA-L-SHADE 算法流程

基于覆盖优先初始化机制和覆盖状态感知参数控制机制，本文构建 CSA-L-SHADE 算法用于求解异构 UAV 集群通信网络动态路由规划问题。算法以 L-SHADE 为基础框架，以 UAV 分段控制序列作为个体编码，通过完整动态仿真获得个体评价向量，并依据覆盖优先、连通次优先、性能再优化的层级判优规则更新种群。其核心流程由三部分组成：首先，利用 CFI 生成覆盖导向初始个体，改善随机初始化的搜索入口；其次，基于种群覆盖状态动态调整 $p$、$F$ 和 $"CR"$；最后，执行 L-SHADE 主循环中的变异、交叉、选择、档案更新、成功历史记忆更新和线性种群缩减。

CSA-L-SHADE 的完整流程如算法 4-1 所示。

#let AlgLine(block) = (block,)

#algorithm(
  title: [CSA-L-SHADE 算法流程],
  {
    AlgLine[输入：$op("MaxNFE")$, $N P_0$, $N P_"min"$, $rho_"CFI"$, $H$]
    AlgLine[输出：最优控制序列 $bold(x)^*$ 及其评价指标]

    Assign[$op("NFE")$][$0$]
    Assign[$ell$][$0$]
    Assign[$cal(A)$][$emptyset$]

    AlgLine[初始化 $M_F$、$M_"CR"$，设置 $S_0 = S_1$，$c_"stab" = 0$]
    AlgLine[基于 CFI 与随机初始化生成初始种群 $cal(P)^(0)$]
    AlgLine[评估 $cal(P)^(0)$，并确定初始最优个体 $bold(x)^*$]

    While(
      [$op("NFE") < op("MaxNFE")$],
      {
        AlgLine[根据线性缩减规则更新 $N P_ell$]
        AlgLine[按层级判优排序并截断种群]
        AlgLine[计算 $g_ell$、$r_ell$、$Delta g_ell$，并更新状态 $S_ell$]

        Assign[$i$][$1$]
        While(
          [$i <= N P_ell$],
          {
            AlgLine[从 $M_F$、$M_"CR"$ 采样 $F_i$、$"CR"_i$]
            AlgLine[根据 $S_ell$ 裁剪 $F_i$、$"CR"_i$，并确定 $p_ell$]
            AlgLine[生成试验个体 $bold(y)_i$]
            AlgLine[修复边界并评估 $bold(y)_i$]

            IfElseChain(
              [$bold(y)_i$ 优于 $bold(x)_i^(ell)$],
              {
                Assign[$bold(x)_i^(ell + 1)$][$bold(y)_i$]
                AlgLine[将被替换个体加入外部档案 $cal(A)$]
                AlgLine[记录成功参数 $F_i$、$"CR"_i$]
              },
              {
                Assign[$bold(x)_i^(ell + 1)$][$bold(x)_i^(ell)$]
              },
            )

            AlgLine[更新全局最优个体 $bold(x)^*$]
            Assign[$i$][$i + 1$]
          },
        )

        AlgLine[更新 $M_F$、$M_"CR"$ 并调整外部档案规模]
        Assign[$ell$][$ell + 1$]
      },
    )

    Return[$bold(x)^*$]
  },
) <alg:csa-lshade>

上述流程中，CFI 只作用于初始种群构造阶段；CSA 机制只作用于参数控制阶段；L-SHADE 主循环仍负责连续控制空间中的差分变异、交叉、选择和历史记忆更新。因此，CSA-L-SHADE 并未改变基础 L-SHADE 的主体演化结构，而是在初始化入口和参数控制层引入覆盖状态信息，使搜索过程能够在覆盖构造、覆盖稳定、性能精修和覆盖恢复之间自适应切换。
== 算法复杂度分析

CSA-L-SHADE 的计算复杂度主要由决策空间维度、个体动态仿真评估和附加算子开销构成。其中，完整任务周期仿真是算法的主要计算瓶颈。

*1. 决策空间维度。*

CSA-L-SHADE 的个体为 UAV 分段控制序列。设 UAV 数量为 $N$，控制段数量为

$
G = ceil(T / L_c),
$

每架 UAV 在每个控制段内包含期望速度、期望航向角和发射功率 3 个变量，因此单个候选解维度为

$
D = 3 N G.
$

由此可知，算法需要在随 $N$ 和 $G$ 线性增长的高维连续控制空间中搜索。

*2. 个体评估复杂度。*

每个候选解均需展开为完整时域动作序列，并在仿真环境中执行 $T$ 个时隙。单个时隙内，UAV-UAV 通信速率计算与安全距离检查主要涉及 $N^2$ 级计算，UAV-地面任务节点覆盖判定与数据到达计算主要涉及 $N K$ 级计算。因此，单次个体评估的主复杂度为

$
O(T (N^2 + N K)).
$

若最大函数评价次数为 $op("MaxNFE")$，则由动态仿真评估产生的主导复杂度为

$
O(op("MaxNFE") dot T (N^2 + N K)).
$

*3. 附加算子开销。*

除动态仿真评估外，算法还包含种群排序、pbest 选择、变异交叉、边界修复、外部档案维护、成功历史记忆更新和线性种群缩减等操作。设第 $ell$ 代种群规模为 $N P_ell$，这些操作主要与 $N P_ell$ 和个体维度 $D$ 有关，通常低于完整时域仿真评估的开销。

CFI 机制只在初始化阶段执行一次，其开销来自覆盖目标选择、UAV-目标匹配和覆盖导向控制序列生成。CSA 机制在每代执行覆盖状态统计、有限状态机转移判断以及$F$/$ "CR"$ 参数裁剪，其中精英覆盖均值、coverage-safe 比例和状态转移判断均属于轻量级种群统计操作，不引入额外的时域仿真过程。

综上，CSA-L-SHADE 的总体计算成本仍由候选解的完整动态仿真评估主导。CFI 和 CSA 机制只带来一次性初始化开销或低阶种群级统计开销，不改变算法的渐进主复杂度阶。因此，CSA-L-SHADE 与基础 L-SHADE 在主阶上保持一致：

$
O(op("MaxNFE") dot T (N^2 + N K)).
$

该结果表明，覆盖状态感知机制能够在不显著增加总体计算复杂度的前提下，引入针对动态覆盖路由问题的结构化搜索引导。
= 仿真实验与结果分析
== 实验设置

为验证本文所提出 CSA-L-SHADE 算法在异构无人机集群动态路由规划问题中的有效性，本文在统一仿真平台下开展对比实验。所有实验均基于第 3 章建立的无人机运动、通信、覆盖、队列与能量模型进行实现，并采用固定环境设置以保证不同算法之间的公平比较。

实验采用固定环境模式，即所有算法共享相同的任务节点分布、无人机初始位置及异构参数配置。实验环境随机种子设为 42，每种算法独立运行 5 次，最大函数评估次数设置为 30000。

实验场景参数如表 5-1 所示。

#table(
  columns: (1fr, auto),
  table.header([参数], [数值]),
  [UAV 数量 $N$], [8],
  [任务节点数量 $K$], [20],
  [仿真时长 $T$], [200],
  [控制段长度 $L$], [10],
  [控制段数 $S$], [20],
  [时隙长度 $Delta t$], [1 s],
  [UAV 飞行高度 $H$], [100 m],
)

为体现无人机异构性，实验中各 UAV 的覆盖半径、最大速度、带宽、最大发射功率、队列容量及初始能量均采用随机异构配置，其参数范围如表 5-2 所示。

#table(
  columns: (1fr, auto),
  table.header([参数], [取值范围]),
  [最大速度 $V_i^"max"$], [[10, 25] m/s],
  [覆盖半径 $R_i^c$], [[150, 300] m],
  [带宽 $B_i$], [[1e6, 5e6] Hz],
  [最大发射功率 $P_i^"max"$], [[1, 5] W],
  [队列容量 $Q_i^"max"$], [[5e6, 1e7] bit],
  [初始能量 $E_i^"max"$], [[2e5, 3e5] J],
)

其余通信模型参数、动力学约束参数以及 CSA-L-SHADE 状态控制参数见附录 B。
== 对比算法与评价指标

为验证本文所提出 CSA-L-SHADE 算法的有效性，实验主要围绕 L-SHADE 系列算法展开对比。前期研究中曾对 GA、PSO、DE 等常见启发式算法进行初步比较，用于验证不同群智能算法在无人机动态路由规划问题中的适用性。综合收敛稳定性与优化效果后，本文选取 L-SHADE 作为后续改进的基础框架。因此，本章正式实验重点比较 L-SHADE、CFI-L-SHADE 和 CSA-L-SHADE 三种算法。三种算法采用统一的差分进化框架与基础参数配置，其初始种群规模为100，最小种群规模为20，历史记忆长度为10，最大函数评估次数为30000。

实验结果采用固定环境下 5 次独立运行的统计结果进行分析，并以均值和标准差形式展示。实验评价指标采用第 3 章定义的综合性能指标，主结果分析重点关注三种算法在 $J_"total"$、coverage ratio、$V_"cov"^"norm"$ 和 $V_"con"^"norm"$ 上的差异；辅助分析进一步结合 $R_"norm"$、$Q_"norm"$ 和 $E_"norm"$ 解释不同算法在吞吐、队列和能耗方面的性能变化。
本文不单独进行显著性检验，而是结合均值、标准差和收敛过程对算法结果进行综合分析。
== 实验结果与分析

本节基于固定环境下 5 次独立运行结果，对 L-SHADE、CFI-L-SHADE 和 CSA-L-SHADE 三种算法进行对比分析。实验重点考察综合目标值、覆盖性能、连通违约、吞吐量、队列负载和能耗等指标，并结合收敛曲线与结果分布分析不同算法的优化特征。

=== 综合指标与覆盖性能对比

表 5-5 给出了三种算法在固定环境下的主评价指标结果。

#table(
  columns: (auto, auto, auto, auto, auto),
  table.header([Algorithm], [$J_"total"$], [coverage ratio], [$V_"cov"^"norm"$], [$V_"con"^"norm"$]),
  [L-SHADE], [$-0.016413 ± 0.017189$], [$0.827400 ± 0.015883$], [$0.172600 ± 0.015883$], [$0.000000 ± 0.000000$],
  [CFI-L-SHADE], [$-0.092238 ± 0.019092$], [$0.859600 ± 0.013599$], [$0.150490 ± 0.011271$], [$0.000000 ± 0.000000$],
  [CSA-L-SHADE], [$-0.112365 ± 0.062400$], [$0.894350 ± 0.025073$], [$0.107630 ± 0.021777$], [$0.000000 ± 0.000000$],
)

从综合目标值看，三种算法呈现出明显的递进关系。L-SHADE 的平均 $J_"total"$ 为 $-0.016413$，CFI-L-SHADE 降低至 $-0.092238$，CSA-L-SHADE 进一步降低至 $-0.112365$。相较 L-SHADE，CFI-L-SHADE 的综合目标值绝对下降 $0.075825$，CSA-L-SHADE 绝对下降 $0.095952$；相较 CFI-L-SHADE，CSA-L-SHADE 仍进一步下降 $0.020127$。由于 $J_"total"$ 为加权综合指标，且 L-SHADE 基线值接近 0，相对百分比容易被放大，因此本文主要结合绝对下降幅度和分项指标进行解释。

覆盖指标进一步说明了算法改进的来源。L-SHADE 的平均 coverage ratio 为 $0.827400$，CFI-L-SHADE 提升至 $0.859600$，CSA-L-SHADE 进一步提升至 $0.894350$。相较 L-SHADE，CFI-L-SHADE 覆盖率提高约 3.89%，CSA-L-SHADE 提高约 8.09%；相较 CFI-L-SHADE，CSA-L-SHADE 仍提高约 4.04%。与之对应，CSA-L-SHADE 的 $V_"cov"^"norm"$ 相较 L-SHADE 降低约 37.64%，相较 CFI-L-SHADE 降低约 28.48%。这说明 CSA-L-SHADE 的优势主要来自覆盖不足程度的显著下降，而不是单纯由目标函数权重造成的数值差异。

三种算法的 $V_"con"^"norm"$ 均为 0，说明在当前固定环境和参数设置下，三种算法最终解均满足基于 Sink 数据泄放能力的功能性回传要求。因此，本组实验中的主要差异集中在覆盖能力、吞吐表现、队列负载和能耗水平上。

=== 通信性能与资源消耗分析

为进一步分析综合目标值改善的来源，表 5-6 给出了三种算法在吞吐量、队列负载和能耗方面的结果。

#table(
  columns: (auto, auto, auto, auto),
  table.header([Algorithm], [$R_"norm"$], [$Q_"norm"$], [$E_"norm"$]),
  [L-SHADE], [$0.282580 ± 0.024343$], [$0.353330 ± 0.037843$], [$0.000416 ± 0.000011$],
  [CFI-L-SHADE], [$0.436780 ± 0.057542$], [$0.291825 ± 0.019460$], [$0.000407 ± 0.000015$],
  [CSA-L-SHADE], [$0.464139 ± 0.141157$], [$0.283919 ± 0.027831$], [$0.000391 ± 0.000010$],
)

从吞吐量看，CSA-L-SHADE 的 $R_"norm"$ 最高，达到 $0.464139$，相比 L-SHADE 提高约 64.25%，相比 CFI-L-SHADE 提高约 6.26%。这说明覆盖性能改善并未以牺牲 Sink 汇聚能力为代价，反而通过更合理的 UAV 空间分布和发射功率组合，提高了 UAV-Sink 链路的整体可达传输能力。

从队列负载看，CSA-L-SHADE 的 $Q_"norm"$ 最低，为 $0.283919$，相比 L-SHADE 降低约 19.64%，相比 CFI-L-SHADE 降低约 2.71%。该结果表明，CSA-L-SHADE 在提升覆盖率的同时，并未造成更严重的数据积压。其原因在于覆盖状态感知机制并非只追求覆盖率，而是在覆盖相对稳定后继续优化吞吐量和队列泄放能力，使覆盖服务与数据回传之间保持较好平衡。

从能耗看，三种算法的 $E_"norm"$ 均较小，差异不如覆盖和吞吐指标显著，但 CSA-L-SHADE 仍取得最低能耗水平。相比 L-SHADE，CSA-L-SHADE 的归一化能耗降低约 6.01%；相比 CFI-L-SHADE，降低约 3.93%。这说明 CSA-L-SHADE 在改善覆盖、吞吐和队列指标的同时，没有引入额外能耗负担。

综合来看，CSA-L-SHADE 的优势并非来自单一指标，而是由覆盖率提升、覆盖违约降低、吞吐量增加、队列负载下降和能耗轻微降低共同作用形成。其中，覆盖相关指标改善最为明显，符合本文算法围绕覆盖状态进行结构化改进的设计目标。

=== 收敛过程与结果分布分析

图 5-1 至图 5-3 展示了三种算法在最终 $J_"total"$、coverage ratio 和 $V_"cov"^"norm"$ 上的分布情况。总体来看，L-SHADE 的最终目标值较高、覆盖率较低、覆盖违约较大；CFI-L-SHADE 在三项指标上均明显优于 L-SHADE；CSA-L-SHADE 进一步取得最低的平均 $J_"total"$、最高的 coverage ratio 和最低的 $V_"cov"^"norm"$。该分布结果与表 5-7 一致，说明算法改进带来的性能提升具有稳定的统计表现。

#capfig(
  image("figures/image5-1.png", width: 80%),
  caption: [三种算法最终综合目标值分布],
  label: <fig:final-j-total>,
)

#capfig(
  image("figures/image5-2.png", width: 80%),
  caption: [三种算法最终覆盖率分布],
  label: <fig:final-coverage-ratio>,
)

#capfig(
  image("figures/image5-3.png", width: 80%),
  caption: [三种算法最终覆盖违约分布],
  label: <fig:final-coverage-violation>,
)


图 5-4 给出了 best $J_"total"$-NFE 收敛曲线。L-SHADE 在搜索早期下降后较快进入平台期，说明随机初始化和通用差分扰动难以持续改善覆盖路由结构。CFI-L-SHADE 在搜索初期即取得更低目标值，表明覆盖优先初始化改善了初始种群质量，使算法能够更早围绕较好的覆盖结构展开搜索。CSA-L-SHADE 在前中期不一定始终优于 CFI-L-SHADE，但在搜索后期继续下降，并最终达到最低目标值，说明覆盖状态感知参数控制增强了算法的持续优化能力。

#capfig(
  image("figures/image5-4.png", width: 80%),
  caption: [三种算法 best $J_"total"$-NFE 收敛曲线],
  label: <fig:best-j-total-nfe>,
)

图 5-5 展示了最终 $J_"total"$ 与 coverage ratio 之间的关系。L-SHADE 样本主要分布在较低覆盖率和较高目标值区域，CFI-L-SHADE 整体向更高覆盖率和更低目标值方向移动，CSA-L-SHADE 则进一步集中在高覆盖、低目标值区域。该结果说明，在本文动态路由规划模型中，覆盖状态不仅是目标函数中的惩罚项，也是数据到达、队列演化和通信性能优化的基础条件。较好的覆盖结构能够为后续 Sink 汇聚、队列泄放和能耗控制提供更有利的系统状态，因此围绕覆盖状态设计搜索控制机制具有明确的模型依据。

#capfig(
  image("figures/image5-5.png", width: 80%),
  caption: [最终综合目标值与覆盖率关系],
  label: <fig:j-total-coverage-ratio>,
)

综上，实验结果表明，CFI-L-SHADE 能够有效改善 L-SHADE 的低覆盖冷启动问题，CSA-L-SHADE 则在 CFI 的基础上进一步增强覆盖保持和后期综合优化能力。三种算法的递进结果验证了本文“覆盖优先初始化 + 覆盖状态感知参数控制”设计思路的有效性。

= 总结与展望

== 主要研究成果与创新点

本文主要研究成果和创新点可概括为以下三个方面。

建立面向异构 UAV 集群的动态覆盖路由规划模型。不同于仅考虑静态覆盖或拓扑连通的模型，本文将 UAV 运动控制、地面任务节点覆盖、链路传输速率、缓存队列演化和能量消耗纳入统一框架，刻画了“轨迹-覆盖-队列-回传-能耗”之间的动态耦合关系。同时，本文采用功能性回传能力表征系统面向 Sink 的数据泄放能力，避免仅依赖静态图论连通性评价网络性能，使模型更符合动态应急通信场景下的数据传输需求。

利用覆盖优先初始化机制。针对原始 L-SHADE 随机初始化缺少任务节点空间信息、搜索早期容易出现低覆盖轨迹的问题，本文设计了基于地面任务节点分布的覆盖导向初始化方法。该机制通过目标点选择、UAV-目标匹配和“接近-保持”式控制序列生成，使初始种群中包含更接近覆盖可行区域的个体。实验结果表明，该机制能够有效提高初始和最终覆盖质量，为后续差分进化搜索提供更合理的搜索入口。

提出覆盖状态感知 CSA-L-SHADE 算法。针对原始 L-SHADE 参数自适应主要依赖历史成功经验、缺少对 UAV 覆盖状态显式感知的问题，本文构造了覆盖状态指标和有限状态机，将搜索过程划分为覆盖构造、覆盖稳定、性能精修和覆盖恢复四类状态，并根据不同状态动态约束 $p$、$F$ 和 $"CR"$。该机制使 L-SHADE 能够根据种群覆盖状态调整搜索尺度和搜索方向，在保持覆盖结构的同时进一步优化吞吐量、队列负载和能耗水平。

== 不足与展望

当前研究仍存在一定不足，后续可从以下几个方面进一步改进。

第一，通信信道模型仍较为简化。本文采用基于二维距离平方衰减的简化信道模型，未显式考虑三维距离、高度变化、复杂地形遮挡、LoS/NLoS 概率切换、多径衰落和同频干扰等因素。后续研究可引入更真实的空地信道模型和干扰模型，使通信速率计算更加贴近复杂应急场景。

第二，当前路由表征仍属于规划层面的功能性回传建模。本文通过 UAV-UAV 链路速率、UAV-Sink 汇聚速率和队列泄放能力刻画动态回传能力，但没有引入显式下一跳选择、链路调度或数据包级路由变量。后续可进一步结合图路由或流量分配模型，将 UAV 轨迹控制与显式路由决策联合优化。

第三，实验场景仍以固定环境验证为主。虽然固定环境有助于保证算法公平比较，但实际应急通信场景中任务节点分布、UAV 初始位置、数据产生强度和通信环境可能具有更强随机性。后续应在多随机环境、多任务规模和不同参数设置下进一步验证 CSA-L-SHADE 的稳定性和泛化能力。

第四，CSA-L-SHADE 的状态控制参数仍依赖人工设定。本文根据覆盖状态设计了参数状态约束范围，但不同场景下最优参数可能不同。后续可研究自适应阈值调整、强化学习辅助参数控制或多状态记忆机制，使算法能够根据环境规模和搜索过程自动调整状态判定条件与参数范围。

综上，本文提出的 CSA-L-SHADE 算法能够有效改善异构 UAV 集群动态路由规划中的覆盖保持能力和综合优化性能，为 UAV 应急通信网络建模与智能优化提供了一种可行思路。未来可在更真实的通信环境、更复杂的路由机制和更强的自适应能力方面继续拓展研究。